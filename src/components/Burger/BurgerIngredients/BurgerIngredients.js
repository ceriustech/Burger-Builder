import React from 'react';

const burgerIngredients = (props) => {
    let ingredient = null; 

    switch (props.type) {
        case('bread-bottom'):
            
    }
};

export default burgerIngredients; 